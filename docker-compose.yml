version: '3.8'

services:
  app:
    build: .
    container_name: tax_navigator_app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./src:/app/src
      - ./app.py:/app/app.py
      - ./data:/app/data
    restart: unless-stopped
